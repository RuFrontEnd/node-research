// terminal 輸入 node 可以進 REPL(read-eval-print loop) => 類似於瀏覽器開發者工具
// terminal 輸入 node "file name" => 執行 js 檔
// global 為 node.js 的頂層物件

const mission = "learn";

if (mission === "learn") {
  console.log("Time to write some Node code!");
} else {
  console.log(`Is" ${mission} "really more fun?`);
}

// Introduction to Backend VS Frontend